# blog.l33chers.de
our blog

## Clone with submodules
Use one of the following so the theme submodule is checked out:
```sh
git clone --recurse-submodules git@github.com:l33chers/blog.l33chers.de.git
```

If you already cloned:
```sh
git submodule update --init --recursive
```
